# Copy this file to .env and configure your AI provider

# AI Provider: "ollama" (default, local) or "anthropic" (cloud)
AI_PROVIDER=ollama

# Anthropic settings (only needed if AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Ollama settings (only needed if AI_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b